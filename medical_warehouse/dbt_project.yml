name: 'medical_warehouse'
version: '1.0.0'
config-version: 2

# This points to the profile layout we created in your user directory
profile: 'medical_warehouse'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  medical_warehouse:
    staging:
      +materialized: view
    marts:
      +materialized: table